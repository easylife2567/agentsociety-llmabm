# 公众人物去世后，算法策展如何塑造舆论场对其的数字表征

## 核心命题

> **谁在决定逝者如何被表达？**——推荐算法通过选择性放大已有的玩梗群体，改变公众人物身后的公共表征（专业老师 vs 地狱笑话符号）。

主题澄清源文件：`TOPIC_1788166337268_0_u31u.md`（2026-08-31，用户确认）；研究焦点修订（2026-09-08，用户裁定）：从"认知偏移"转向"推荐算法选择性放大已有的玩梗群体 → 改变公共表征"。

## 案例与现象

**案例**：张雪峰老师（2026-03-24 心源性猝死，真实事件非假设）。

逝后舆论场出现**数字表征转移**：从以专业内容为中心的理性讨论（"考研老师"表征），转向以死亡方式为符号的迷因化娱乐消费（"跑步猝死的梗"表征），并跨圈层扩散。

宏微观衔接：

- **宏观结果**：舆论场表征构成随时间的转移（专业内容 ↔ 迷因符号的此消彼长）——数据实证已由真实打标数据周级曲线证实："教育 → 悼念 → 玩梗"三阶段转移（W13 悼念 44% 峰值 → W19–W22 玩梗 9%→63%）；
- **微观机制**：群体层面的选择性放大——玩梗群体在事件前已存在于平台生态，推荐算法按互动信号系统性提升其可见度；改变的是各类群体被看见的比例（构成效应），不以个体认知偏移为中介；
- **策展机制**：推荐系统作为信息环境组织者，决定哪种表征被放大、哪种被抑制。

## 方法论骨架

- **生成性验证**：仿真要尽可能多复现真实现象形态，每个参数有机制依据，不黑箱调参（效标 = 已数字化的 benchmark 周级曲线 `hypothesis_4/benchmark_curves.json`：W13 悼念峰、W19 起玩梗二波；生成性验证由 interest 臂兼任）。
- **因果识别**：算法反事实——全历史均匀随机 / 小时级时序推荐 / 兴趣推荐**三臂对照**。random 是强去策展反事实：每周从截至当周 Env 已出现的全部帖子中全 10 槽均匀无放回抽样，不使用生命周期、时间、兴趣、热度、累计曝光、官方置顶或 W13 哀悼保底。chronological 是独立的小时级同步微批次制度：真实帖按精确 `published_at` 到达，每个 Agent 依事件前真实的星期×小时分布每周行动一次，同小时互不可见、跨小时可见，候选池为行动时刻前全部已发布帖子并按时间倒序取最新 10 条；同样不使用生命周期、兴趣、热度、置顶或保底。interest 保留自己的生命周期与兴趣比例抽样。三臂比较识别的是三种完整策展制度的差异，而非同候选池下的纯排序效应。
- **反事实来源**：推荐算法三臂对照（random-global / chronological / interest）做机制归因；玩梗涌现环境开关（sustained_hot）随 G 退役失效，其证据作用由已完成的 no-G 探针 run 承担。
- **行为建模**：LLM 驱动（给 agent 群体人设 + 约束，LLM 决定表达），非规则驱动——PersonAgent persona + 曝光层由推荐算法参数化 + 表征层从群体互动中涌现（评论必须引用所见内容，禁止凭空生成）；
- **群体生成**：自下而上——以真实打标数据的类型比例生成 100 个固定类型 Agent（2026-09-10 用户裁定按**发帖人口径**；2026-09-13 更正为营销 23/悼念 22/其他 21/玩梗 19/教育 15；类型固定有实证支撑）；

## 选择性放大链（机制时间线，6 环节）

1. 事件触发期（公众关注涌入，各内容类群被激活）→ 2. 群体生产期（各群体按既有倾向生产内容；玩梗群体就地转向事件素材）→ 3. 互动信号积累期（梗内容因高情绪、高可复制性获得超额互动）→ 4. 算法放大期（热度加权推荐将高互动梗内容推至更大曝光面，玩梗群体曝光份额超过其供给占比）→ 5. 表征构成转移期（玩梗群体可见度挤压其他群体的表征空间，公共表征向梗符号倾斜）→ 6. 表征固化期（公众可及的逝者表征被梗符号主导）

> 仿真操作化说明（2026-09-08 用户裁定）：仿真中"算法放大"以**纯兴趣匹配**（类型词表×帖子特征，无热度项）为最小机制实现——检验个性化选择性曝光本身能否差异化激活既有玩梗群体；若不能涌现转移，则反证热度信号是放大的必要成分。

## 方法

多智能体仿真（ABM）：仅设定微观规则（智能体行为规则、平台推送规则、内容互动规则），宏观表征转移由智能体自主交互涌现、不预设结果；反事实通过切换/关闭推荐逻辑（去策展化）实现机制归因；效度检验遵循模式导向建模（表面效准、模式匹配、敏感性分析），若仿真未涌现预期现象则返回调整初始规则，而非强行修改结果。

**数据驱动设定**：agent 群体与供给池均来自真实打标数据（`抖音微博小红书-全量已打标.xlsx`，52736 行，工作区根目录；用户图解读 `图表解读.docx`）：5 内容类群（内容口径：借势营销 30.6% / 事件悼念 17.0% / 其他讨论 16.3% / 教育观点 14.7% / 梗文化 13.6% / 爬取噪音 7.6% 不单独作为 Agent）；Agent 比例按**发帖人口径**（玩梗 18 / 悼念 21 / 营销 26 / 教育 15 / 其他 20，2026-09-10 用户裁定；旧用户口径主导类型 41/21/13/13/12 随口径切换废弃）；

## 规模预算

100 个固定类型 Agent，tick = 周，11 tick/run（W12 生前基线周 → W22，对齐真实数据窗口），3 推荐算法 × 3 seeds = **9 runs**（≈0.99 万 agent-ticks；2026-09-12 用户裁定由 3×2 全因子降为单因子）。

## 当前表达效用版本（2026-09-14）

新一轮 `anchored_v1` 保持原两条行为机制与单因子三臂设计不变，仅将组合公式改为 `U_i=B_i+R_i(D_i−B_i)`。常态锚点 `B_i` 固定自 W05–W12；全局 `alpha_D` 与五类疲劳系数 λ 只用 W13–W18 标定，W19–W22 留出。该结构让注意力衰减消除事件冲击后回到事件前常态盘，而不是把表达效用衰减到零。

## 假设结构

| 假设 | 研究问题 | 对应理论 | 状态 |
|------|----------|---------|------|
| [hypothesis_4](hypothesis_4/HYPOTHESIS.md) | **主假设**：推荐算法选择性放大已有的玩梗群体（其曝光份额超过供给占比），驱动公共表征沿"专业→悼念→迷因"转移 | 算法策展/算法放大 | 已注册；2026-09-14 random 修订为全历史均匀抽样，chronological 修订为独立小时级同步微批次，interest 保持原定义；仍为**单因子 3 臂 × 3 seeds = 9 runs** |
| 基线生成性验证 | 仿真复现"哀悼→玩梗"表征转移形态（拟合真实曲线） | 数字哀悼 | 作为主假设的生成性验证前提，由 interest×normal cell 兼任；效标曲线已数字化（`hypothesis_4/benchmark_curves.json`） |
| 事件/无事件嵌套 | 剥离去世净效应（内部因果） | 方法论设计 | 线索，未注册 |
| 策展类型差异 | 不同策展逻辑下记忆沉淀的差异 | 集体记忆 | 线索，未注册 |
