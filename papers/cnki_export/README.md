# CNKI/万方 中文文献补检 — 执行方案（v0.1）

> 目的：弥补 phase2 文献调研的中文证据缺口（CNKI/万方 此前未系统检索）。
> 前置：Playwright MCP 已注册（`.mcp.json`，项目级）；需要用户在本机浏览器完成一次学校 SSO 登录。
> 状态跟踪：检索完成后并入 `papers/literature_index.json`，并更新流水线 `literature_search` 元数据。

## 检索式（6 组，CNKI「主题」检索）

| # | 检索式 | 目的 |
|---|---|---|
| q1 | `主题=(网络悼念 OR 数字遗产 OR 数字来生 OR 身后身份 OR 数字死亡)` | 中文数字哀悼研究 |
| q2 | `主题=(名人去世 OR 公众人物去世) AND 主题=(社交媒体 OR 短视频 OR 抖音)` | 本土名人死亡传播 |
| q3 | `主题=(张雪峰 OR 公众人物) AND 主题=(算法推荐 OR 信息茧房 OR 流量)` | **个案本土研究（重点）** |
| q4 | `主题=(短视频) AND 主题=(去语境化 OR 碎片化叙事 OR 标签化)` | 短视频叙事机制 |
| q5 | `主题=(算法推荐 OR 推荐算法) AND 主题=(重复曝光 OR 内容同质化 OR 回声室 OR 茧房)` | 算法曝光机制中文版 |
| q6 | `主题=(首因效应 OR 第一印象 OR 印象形成) AND 主题=(社交媒体 OR 网络)` | 首因/印象中文版 |

规则：
- 每组的全部检索结果导出为**一个文件**：`papers/cnki_export/q1.txt` … `q6.txt`。
- 单组超过 500 条时，与用户确认后再决定限年限或限「学术期刊」。
- 万方：优先补 q1/q2 的**学位论文**（CNKI 学位论文库导出受限时的替代来源）。

## 导出格式要求

- 首选 CNKI「导出与分析」→ **RefWorks 或 NoteExpress 格式**（含题名/作者/来源/年份/摘要/关键词）。
- 若无上述格式，用「自定义导出」，勾选：题名、作者、来源、年份、期号、摘要、关键词。
- 保持 UTF-8 / 原始编码均可，解析脚本统一处理。

## 流程（Playwright MCP 就绪后）

1. `browser_navigate` 到 CNKI 检索页（如 https://kns.cnki.net/kns8s/defaultresult/index ）。
2. 若未登录：提示用户在**打开的浏览器窗口**里完成学校 SSO 登录（密码不经手 Claude）。
3. 逐组执行 q1–q6：输入检索式 → 检索 → 全选结果 → 导出 → 下载文件（playwright MCP 的下载处理）→ 重命名为 `qN.txt` 存入 `papers/cnki_export/`。
4. 万方（可选）：学位论文补检 q1/q2。
5. 解析：标题规范化去重（与现有 67 条比对）→ 题录核验（DOI/作者/年份/期刊）→ 按相关性分级（core/supporting/peripheral）→ 追加进 `papers/literature_index.json`（source=cnki_wanfang_round1）。
6. 更新流水线元数据：`research-pipeline update-stage literature_search completed --metadata '{"cnki_round1": ...}'`。
7. `git add -A && git commit`。

## 与本项目协议的一致性

- 纳入/排除标准沿用 `research/停止更新公众人物的算法推荐与公众认知/phase2_文献调研/00_检索协议_v0.1.md`。
- 未取得全文的条目只登记题录（selection_status=title_abstract），不声称原文结论。
- 中文文献同样记录：证据等级（如适用）、综合评级、COI 声明状态（未定位 ≠ 无）。

## 执行状态（2026-08-19 更新）

| # | 检索式（专业检索语法） | 结果 | 文件 | 口径 |
|---|---|---|---|---|
| q1 | `SU=(网络悼念 + 数字遗产 + 数字来生 + 身后身份 + 数字死亡)` | 453 | `q1.txt` | **学术期刊**（>500 时按 README 规则限期刊；实际 453 条按 q1 决定采用期刊口径） |
| q2 | `SU=(名人 + 明星 + 公众人物) * (社交媒体 + 短视频 + 抖音) * (去世 + 死亡 + 逝世 + 离世)` | 9 | `q2.txt` | 总库（仅 9 条，限期刊只剩 1 条，违背补检目的） |
| q3 | `SU=(张雪峰 + 公众人物) * (算法推荐 + 信息茧房 + 流量)` | 17 | `q3.txt` | 总库 |
| q4 | `SU=短视频 * (去语境化 + 碎片化叙事 + 标签化)` | 45 | `q4.txt` | 总库（含 2 条图书章节手工补录，见下） |
| q5 | `SU=(算法推荐 + 推荐算法) * (重复曝光 + 内容同质化 + 回声室 + 茧房)` | 809（导出 804 唯一，见限制 11） | `q5.txt` | 近 5 年（≥2021；>500 时用户决策：限近 5 年全部类型）。656 条 = 期刊 444 / 报纸 21 / 会议 14 / 学位论文 177 |
| q6 | `SU=(首因效应 + 第一印象 + 印象形成) * (社交媒体 + 网络)` | 77 | `q6.txt` | 总库（77 条全量：期刊 51 / 学位论文 18 / 会议 4 / 报纸 2 / 图书 2 全部导出，图书含完整元数据无需补录） |

### 已知限制与经验（后续轮次必读）

1. **口径差异**：q1 采用学术期刊过滤（检索后筛选），q2–q4 为总库口径。合并入索引时按查询式记录，不强行统一。
2. **导出中心 tab 生命周期**：每次 NoteExpress 导出前必须**关闭旧的导出中心 tab**，否则新 tab 载入旧批次（q2 踩坑）。
3. **"已选"跨检索持久化**：结果页 Vue store 的已选集合跨检索保留（q1 的 453 条残留）。每组检索勾选前先刷新页面/清空。
4. **图书章节（心可书馆 uniplatform=XKPT）稳定丢失**：NoteExpress 批量导出跳过图书章节类记录（q4 的「第4章 短视频发布」「第8章 短视频内容标签化」两次导出均缺失，43/45）。需在结果页打开章节详情页（thinker.cnki.net/wbfd/chapterdetail）手工补录元数据（著者/出版社/ISBN/页码）。
5. **页码链接**：结果页分页条中的页码（如 "3"）是普通链接（复用以第一条结果 URL 为 href 的 abstract 链接），点击会开新 tab 而非 AJAX 翻页。AJAX 翻页请点击分页条中的数字/「下一页」（`javascript:void(0)` 链接）。
6. **下载文件命名**：`CNKI-YYYYMMDDHHMMSSmmm.net`，落在 `.playwright-mcp/`，解析后存入 `papers/cnki_export/qN.txt`。
7. **NoteExpress `{Title}` 截断副标题**：导出标题只保留主标题（结果页「A——B」→ 导出「A」），与结果页逐条比较会产生假性「缺失」。比对必须用 (title, author) 对，且比较前从**两侧**剥离副标题（`——` 之后）与标点。
8. **结果页分页是动态窗口**：分页条只渲染约 7 个页码按钮（如 `首页 上一页 [37][38][39][40][41(当前)] 下一页 尾页`），跳转指定页码只在该数字可见时有效，否则点击静默失败。可靠翻页：重复点击「上一页」直到当前页 ≤ 目标页。
9. **选中节奏**：点 checkbox 后等 800ms、页面变化后等 1600ms 再继续；400/1200ms 会丢记录（实测 478/500、287/309）。
10. **报纸记录年份在 `{Date}`**：`{Year}` 对报纸为空（如 2026-07-09），年份提取必须依次检查两个字段。
11. **命中数 vs 渲染数 vs 导出数**（q5）：声称 809，渲染 810 行（含重复渲染），NoteExpress 导出并去重后 804 条 unique。校验以**导出集合为准**：结果页抓取标题逐一匹配导出记录（规范化 + 副标题剥离后 missing=0）即视为覆盖完整。q5 的 1 条 XKPT 图书（类型筛选中「图书 1」）被 NoteExpress 跳过，与 q4 模式一致，需手工补录（见 q5 图书补录项）。
12. **腾讯拼图验证（风控）**：跨多轮连续自动化操作会触发拼图验证弹窗（tencent-captcha），需用户手动完成（验证码不经手 Claude）。弹窗关闭后 DOM 残留（移出屏幕，y≈-1000000），不阻塞后续操作。**重新加载 AdvSearch 会重置为「高级检索」模式**：`querySelector('textarea')` 会匹配到隐藏的「专业检索」输入框，此时点「检索」报「请输入检索词」；必须先点击「专业检索」tab 再填式提交（提交按钮为可见的 `input.btn-search`）。
13. **全选 `#selectCheckAll1` 只作用于当前页**：>20 条时需逐页「翻页 → 全选 → 下一页」循环；已选计数跨页累积（q6：20→40→60→77），结束后与结果总数核对。结果页存在同名重复记录（如 q6「精彩有你」×9 为多期连载），导出保留全部。
