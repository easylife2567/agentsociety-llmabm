---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 3358352132278187_0-drive/217175499627774289/EXPERIMENT_CONSTITUTION_1788335113228_0_gm0h.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 2156560362253756#1788339126113
    ReservedCode2: ""
---
# 实验设计宪法

> 项目专用版 v1.0（2026-09-02）。用于约束“公众人物去世后，算法策展如何塑造舆论场对其数字表征”的 LLM-ABM 设计、运行、验证和报告。本文是实验放行规范，不是结果解释模板；实验结果不能反过来修改已经冻结的规则。

## 一、研究对象与可识别范围

### 核心研究问题

谁在决定逝者如何被表达：参与度敏感的信息排序是否会放大迷因化表征，并挤压公众人物原有的专业表征？

本项目区分三个层次：

1. **供给层**：哪些专业、悼念、迷因等内容在何时进入信息环境；
2. **策展层**：平台如何选择、排序和重复曝光这些内容；
3. **认知与表达层**：agent 在实际曝光后如何形成印象并互动或再生产内容。

实验必须明确操纵和测量的是哪一层，不得把三个层次合并成不可解释的“算法效应”。

### 当前正式假设

- **Hypothesis 3（群体差异）**：相同供给、排序和社会环境中，初始低熟悉度群体与高熟悉度群体的认知偏移存在系统差异。方向由数据裁定，不写入行为规则。
- **Hypothesis 4（策展增量效应）**：相对于时间倒序，互动热度敏感排序会提高迷因内容的曝光与互动集中度，并提高 agent 层迷因化认知偏移。

Hypothesis 4 识别的是“互动热度敏感排序相对于时间倒序排序的增量效应”，不等同于“有算法 vs 无算法”。时间序、候选池和 Top-K 截断也构成信息策展；除非另建完整暴露或无排序基准，不得把 chronological 称为完全“无策展”。

### 允许的结论边界

若通过全部质量门，最多可以声称：

> 在冻结的单事件、单模型环境及给定供给条件下，互动热度敏感排序对迷因化数字表征转移产生了可重复的增量影响；或未观察到超过预设实质阈值的增量影响。

不得仅凭本实验声称算法是现实世界唯一原因、排除了全部替代机制，或证明了所有平台的一般规律。

## 二、条款等级

| 等级 | 含义 | 未满足时的处理 |
|---|---|---|
| **G0 阻断门** | 实现、时间或测量完整性的前置条件 | 禁止正式运行或禁止解释结果 |
| **L1 硬性条款** | 科学识别与报告成立的必要条件 | 结果只能标为探索性或无效 |
| **L2 冻结选择** | 必须预先选择一个合理方案 | 选择前不得运行；选择后不得看结果更换 |
| **L3 稳健性条款** | 增强外推性或解释力 | 可不完成，但必须披露 |
| **LLM 特有** | LLM-ABM 独有风险 | 与其 G0/L1/L2/L3 等级同时适用 |

证据、参数和文献依据必须可追溯。尚未核验全文的文献只能列为线索，不能单独支撑硬性裁决或精确数值声称。

---

## 三、正式运行前的阻断门

### G0-1 操纵完整性门

必须用确定性最小用例自动证明：

1. reddit_hot 臂实际调用热度排序，chronological 臂实际调用时间倒序；
2. 日志逐次记录 run、tick、agent、实际算法、候选帖和返回顺序；
3. 已知点赞数和时间戳的测试帖子在两算法下产生预期且可区分的排序；
4. 两臂除排序机制外的 agent、供给、时钟、prompt、模型和参数完全一致；
5. 算法回退必须显式报错，不能静默继续。

配置中出现不同字符串不算通过。若两臂实际 feed 无可验证差异，Hypothesis 4 不可检验。

### G0-2 时间与信息完整性门

必须满足：

1. 候选池只包含 created_at 不晚于 simulation_time 的帖子；
2. 去世前、事件冲击、悼念、二创和迷因扩散内容按冻结时序进入环境；
3. 真实周 W13–W22 与 12 个仿真 tick 的压缩映射在运行前冻结；
4. 每 tick 保存新注入内容的 ID、类型、来源时点和数量；
5. 未来帖子可见、素材一次性全量泄漏或注入失败均为阻断错误。

### G0-3 测量与效标冻结门

正式运行前必须冻结：

- benchmarks.json 或等价基准文件；
- 每一真实周的专业、悼念、迷因及其他表征占比；
- 关键拐点、容许时间窗、缺失规则和样本量；
- tick 到真实周的映射；
- 表征编码本体和模糊案例规则；
- 主指标、次指标、最小实质效应阈值和判定方向；
- 校准数据与验证数据的边界。

若 persona、供给池和 benchmark 来自同一数据集，必须建立**校准—验证防火墙**。用于构造模型或选择参数的数据不能再次作为独立验证证据。优先留出时间窗、平台或样本；无法留出时，将验证降级为样本内模式复现并披露循环性。

基准未冻结时，只允许实现测试和小规模试点，不允许宣称基线效度或正式因果结论。

---

## 四、规则与模型设计

### L1-1 结果必须涌现

环境只能注入外生条件：真实素材、事件时点、排序逻辑和资源约束。不得硬编码某 tick 迷因反超、目标占比、必然互动或预定认知结论。迷因何时上升、是否反超、哪类群体偏移更大，必须由供给、曝光、互动和记忆共同产生。

### L1-2 参数必须有独立来源

- agent 特征与分布来自冻结数据分析；
- 供给内容和时序来自冻结真实记录；
- 交互预算、沉默率和生产率来自数据、理论或明确标注的建模假设；
- 无数据依据的参数登记为不确定参数并进入敏感性分析；
- 禁止为了对上反超时点进行未登记调参。

每个参数记录名称、范围、来源、冻结日期、可否校准及敏感性分析去向。

### L1-3 假设可证伪，但不以“是否反超”为唯一标准

Hypothesis 3 检验两组认知偏移差及其不确定性。Hypothesis 4 检验热度臂与时间序臂的配对增量效应。主要效应未达到预设方向和实质阈值时，不支持对应方向性假设；这不自动证明算法完全无效或供给端是唯一原因。

即使所有实验臂都出现迷因反超，反超时点、幅度或 AUC 的稳定差异仍可能表示增量作用。不得用二元“反超/未反超”替代连续效应估计。

### L1-4 反事实用于因果分析，不用于现实效度

- 真实事件 + 热度排序臂可承担模式复现；
- 时间序、无事件和随机排序没有直接现实 ground truth，只用于机制比较；
- 反事实可淘汰不敏感机制，不能自动选择唯一真机制；
- 无事件组出现迷因首先是机制诊断信号。仅当输入明确定义为不含死亡相关内容且输出违反约束时，才判为实现错误。

### L1-5 多重约束必须独立

基线复现至少覆盖时间形状、群体结构、素材聚焦和逝前基线。高度同源指标不得重复计为多重验证。跨平台差异只能作为平台异质性的描述证据，不能单独证明算法因果作用。

### L1-6 人设只规定深层倾向（LLM 特有）

允许写入有数据支持的亚文化符号亲和、情绪风格、初始熟悉度和信息兴趣；禁止写入“看到梗就整活”“一定评论”“会形成某种印象”等目标行为。无数据支持的深层动机应删除或标为理论假设。persona 必须经过关键词扫描和人工抽查。

### L1-7 Prompt 等价扰动（LLM 特有）

冻结一个主 prompt，并准备至少两个**语义等价、格式不同**的版本，如散文、列表和键值格式。“看到有趣内容”“看到高互动内容”“看到死亡相关内容”改变了机制，不能当作等价措辞。

Hypothesis 4 至少在主版本与两个等价格式版本上复核。效应只在单一格式存在时，结论降级为 prompt 依赖的实现现象。

---

## 五、初始化、变量与统计单位

### L1-8 前提、初始化、操纵与结果分离

方法节必须分别说明：脚手架前提（稳定 persona、有限注意力、记忆）、初始化（群体比例、熟悉度、供给池）、实验操纵（排序机制）和待解释结果（曝光、互动、再生产、认知变化）。前提和初始化不能当作研究发现。

### L1-9 主因变量必须测量表征方向

**Hypothesis 4 宏观主指标：**

1. 每 tick 的迷因曝光份额；
2. 每 tick 的专业/教育曝光份额；
3. 迷因曝光份额减专业曝光份额的 T0→T3 变化或时间 AUC；
4. 热度臂相对时间序臂的配对差。

互动份额、转发份额、HHI、Top-1 和反超时点为次指标。HHI 只能说明集中程度，不能说明集中到哪种表征。

**Hypothesis 3 微观主指标：**

- 每名 agent 相对 T0 的迷因化印象变化；
- 专业身份表征变化；
- 职业事实层偏差；
- 低熟与高熟群体的变化差。

印象词按冻结本体编码。若由 LLM 编码，保留盲化人工复核样本、编码一致性和争议记录。

### L1-10 实验运行是主要统计单位

同一 run 内的 100 个 agent 共享环境，不能视为 100 个独立重复。主要推断单位是独立运行，agent 和 tick 是嵌套/重复测量层。

Hypothesis 4 采用配对设计：相同 persona、供给、框架 seed、prompt 和模型分别运行 reddit_hot 与 chronological。每次配对的主要估计量为：

    tau_r = 热度臂表征变化 - 时间序臂表征变化

报告 tau 的均值、区间、Monte Carlo 标准误、方向一致率和时间动态，不得用 agent 数量虚增自由度。

### L2-1 沉默建模方式必须冻结

正式运行前选择并说明依据：agent 主动选择 speak/silent，或使用有数据/理论依据的低概率表达模型。沉默规则不能在看过结果后更换。与 group-agent 粒度不匹配的个体统计只能作为初始化线索或局限。

---

## 六、试点与正式运行

### G0-4 试点门

前三道阻断门通过后，先执行 3–5 对热度/时间序配对试点。试点只用于验证链路、估计运行成本与失败率、估计运行间方差，并确认日志可追踪“曝光→互动→认知”。

试点后允许修复实现错误和不可运行的工程参数，但不得依据效应方向调到“符合现实”。修复必须记录并重新冻结；调试试点不得并入确认性结果。

### L1-11 重复数由精度和预算决定

根据试点方差决定正式配对重复数，并预先记录目标区间宽度或 Monte Carlo 标准误、最小实质效应、最大预算、失败运行处理和停止规则。预算不足时结果标为探索性。

框架 seed 在两臂中配对。服务端 LLM 随机性无法完全控制时，报告框架随机与 LLM 随机的总变异，不声称完全确定性复现。

### L1-12 敏感性分析针对实际自由参数

只有真正进入运行时的参数才能扫描。若 reddit_hot 没有可配置热度权重，就不能声称完成“低/中/高热度权重”扫描；应先明确扩展模型及理论含义，或比较预先定义的排序算法。

优先扫描策展参数、曝光上限、重复曝光阈值、沉默参数和供给参数。敏感性分析用于刻画效应范围，不用于调出目标曲线。

### L3-1 温度与模型稳健性

主实验冻结模型版本和温度。资源允许时，用预注册温度档或另一模型族复核效应方向；稳健性结果不能与主实验混合估计。

---

## 七、验证与因果分析

### L1-13 实现验证优先

自动测试并保存工具注册、实际排序、时间过滤、素材注入、曝光日志、互动计数、问卷时间点、输出完整性和失败回退证据。语法/配置校验不能替代逻辑验证。

### L1-14 基线模式复现

事件 + 热度排序基线是当前唯一可与真实曲线比较的臂。主分布距离使用预先选择的定量指标，如按 tick/周计算 JS 散度；波峰位置仅作辅助。若只能样本内复现，使用“模式复现”而非“外部效度验证”。

### L1-15 Hypothesis 4 反事实分析

成立证据来自预注册方向性效应及其区间，而不是简单要求“热度组反超、时间序组不反超”。解释顺序是：操纵生效→曝光分离→互动/再生产改变→认知和宏观表征改变→效应达到实质阈值且跨重复稳定。

未观察到效应时，依次区分操纵失败、精度不足、测量不敏感和机制不支持，不得直接跳到“算法无效”。

### L1-16 Hypothesis 3 群体分析

低熟与高熟 agent 必须在同一 run、同一供给和同一排序下共存。T0 熟悉度是操纵检查；设计分组与自评严重不一致时，先报告分组失效。分析比较相对 T0 的变化并在运行层汇总组间差。相反方向是可报告结果，不得事后改写假设。

### L3-2 微观—宏观链条一致性

检查“曝光差异→互动/再生产差异→认知差异→宏观表征差异”的时间和方向。某层不响应时，将其视为机制链断点，不能只凭最终曲线宣布机制成立。

### L3-3 后续反事实与多事件

无事件、素材时机、倾向抹平、二创抑制、随机排序、兴趣排序和其他公众人物事件属于后续机制分解或稳健性研究；除非另行注册，不属于 Hypothesis 3/4 核心确认性实验。

---

## 八、报告与审计

### L1-17 结论措辞

报告效应大小、区间、方向一致率、失败路径和适用条件。使用“在模型条件下支持/不支持”“观察到增量影响”，避免“证明了”“排除了”“算法决定了一切”。

### L1-18 完整复现披露

归档代码 commit；配置、种子和运行 ID；LLM provider、精确模型 ID/版本、温度；完整 prompt；persona 与供给池生成方法；benchmark、编码本体和分析脚本；排除、失败、重跑和中止记录；API 调用量、成本、运行时间及所有偏离预注册的变更。

### L1-19 ODD + LLM 扩展

方法报告采用 ODD（Overview、Design concepts、Details），并增加 prompt、模型、记忆、工具、采样随机性、内容安全、失败回退和外部 API 依赖。ODD 是正式复现要求，不是可选美化。

### L1-20 审计状态不得混用

- IMPLEMENTATION_VERIFIED：只证明实现与操纵正确；
- PILOT_ANALYZED：试点完成，不是确认性证据；
- PATTERN_REPRODUCED：冻结基线模式达到预设标准；
- CAUSAL_COMPARISON_ANALYZED：反事实比较完成并报告不确定性；
- REPRODUCIBILITY_VERIFIED：独立重跑符合预设容差。

不得用“配置检查通过”替代上述状态。

---

## 九、正式运行放行清单

| 门槛 | 通过条件 | 当前项目状态（2026-09-02 审查） |
|---|---|---|
| G0-1 操纵完整性 | 运行时算法与 feed 排序可验证地区分 | **未通过**：需确认配置字段实际进入 refresh_feed |
| G0-2 时间完整性 | 无未来信息；按 tick 注入或过滤 | **未通过**：候选池需增加仿真时间约束 |
| G0-3 测量冻结 | benchmark、主指标、阈值、编码规则冻结 | **未通过**：benchmark 尚待数字化 |
| Persona 审计 | 不包含目标表层行为 | **未通过**：当前 persona 存在“看到梗就整活”等措辞 |
| H3 分组检查 | T0 熟悉度与设计分组一致 | 待试点 |
| G0-4 配对试点 | 3–5 对完成，方差/成本/失败率已估计 | 未开始 |
| 正式重复数 | 按精度、实质阈值和预算冻结 | 未决定 |

前三个 G0 门和 persona 审计通过前，即使流水线处于 run_experiment，也只能执行确定性实现测试，不能启动可解释为正式证据的 LLM-ABM 运行。

## 十、依据与核验纪律

方法论线索包括生成性社会科学、ABM 验证与确认、间接校准、ODD、微观—宏观桥接及 LLM prompt 稳健性研究。正式论文引用前必须逐条核验作者、年份、题名、版本和原文支持范围；本文不把未核验的精确数值或二手摘要当作已确认事实。

---

> 本文件由原 AIGC 草案经项目上下文审查后修订。使用与传播时保留 AIGC 标识、版本记录及人工/模型审查说明。
