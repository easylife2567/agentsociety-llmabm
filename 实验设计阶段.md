# 实验设计阶段

## 一、初始设定

### 1. Agent 群体划分

以“抖音微博小红书-全量已打标.xlsx”中的真实数据为基础，根据五类内容的**用户口径主导类型比例**构建 Agent 群体，共设置 **100 个 Agent**。

Agent 分为五种类型：

① 玩梗型 Agent\
② 悼念型 Agent\
③ 教育观点讨论型 Agent\
④ 借势营销型 Agent\
⑤ 其他类型 Agent

Agent 数量按真实数据中五种类型的**用户主导类型比例**分配（2026-09-08 裁定，替代原“按内容比例”表述）。依据 xlsx `author_handle` 字段（n=14,313 用户），剔除爬取噪音后归一化：

| Agent 类型 | 用户主导类型占比 | Agent 数 |
|---|---|---|
| 玩梗型（梗文化讨论） | 40.5% | 41 |
| 悼念型（事件悼念讨论） | 20.8% | 21 |
| 借势营销型 | 13.5% | 13 |
| 教育观点讨论型 | 13.5% | 13 |
| 其他类型 | 11.7% | 12 |

选择用户口径而非内容口径的理由：观测指标“群体规模 → 发言比例 → 供给比例 → 曝光比例”漏斗的第一环是群体规模；内容口径会把营销号“人少帖多”（12.6% 用户产出 30.7% 内容）的活跃度差异误置为群体规模差异。

根据数据分析结果，仅约 4% 的用户存在明显跨类型表达（主导类型占比 <80% 的用户仅 3.88%），因此本实验将 Agent 类型视为相对稳定的表达倾向。每个 Agent 在整个实验过程中保持其初始类型不变。

该设计的目的不是模拟个体从一种认知或表达类型向另一种类型转变，而是研究：

**在不同信息分发环境下，不同既有表达群体的可见性和表达活跃度如何发生变化，并进一步改变舆论场中的公共数字表征。**

### 2. Agent 类型词表

为五种 Agent 分别建立对应的语义词表或兴趣词表。

词表具有两个主要作用：

① 表征不同 Agent 的内容兴趣，用于兴趣推荐算法计算 Agent 与帖子的匹配程度；\
② 为 Agent 判断帖子类型、内容相似度以及历史曝光情况提供辅助信息。

词表原则上来源于真实打标数据中的高频词、关键词及具有代表性的语义表达。

---

## 二、Agent 规则

Agent 的行为规则主要分为两个层面：

1. 是否发言；
2. 发言后产生什么类型的内容。

### 1. 发言倾向

Agent 在每一轮接收到 Env 推荐的帖子后，根据当前信息环境决定是否进行公开表达。

发言倾向主要受到以下三个机制影响。

#### ① 沉默的螺旋

Agent 根据当前所接触到的帖子和其他 Agent 的公开表达，形成对当前“意见气候”的感知。

基本机制为：

**感知意见气候 → 判断自身类型是否处于少数或非主流位置 → 感知潜在社会孤立风险 → 降低公开表达意愿。**

例如，当 Agent 感知到当前公共讨论主要是严肃悼念，而自身属于玩梗型 Agent 时，其公开表达意愿可能降低。

相反，当 Agent 感知到与自身类型相同或相近的公开表达逐渐增多时，其表达意愿会上升。

沉默只影响 Agent 是否公开发言，不改变 Agent 本身的类型。

因此：

**Agent 的私人表达倾向保持稳定，但其公开可见性可以随意见气候发生变化。**

这一规则主要用于模拟不同群体在舆论环境中的“表达—沉默”差异。

---

#### ② 注意力衰减

Agent 对重复或高度相似内容存在注意力衰减。

当 Agent 在前几个 tick 中已经多次接触某一类型或高度相似的帖子时，再次看到类似内容时，其继续参与讨论的概率下降。

基本机制为：

**重复曝光增加 → 内容新颖性降低 → 注意力下降 → 发言概率下降。**

不同类型内容可以具有不同的注意力衰减速度。

例如：

- 悼念类内容可能在事件发生初期高度集中，但随着重复曝光快速产生讨论疲劳；
- 玩梗类内容可能由于不断产生新的表达形式，在一定时间内维持较高的新颖性；
- 教育观点类内容可能具有相对稳定、较慢的衰减过程。

不同类型的具体衰减函数和参数将在后续参数设定与敏感性分析阶段确定。

---

#### ③ 玩梗涌现环境（2026-09-10 机制重设计：取代悼念规范压力）

用户 2026-09-10 裁定的存量/流量语义：舆论场总帖子越丰沛越易诞生 meme（存量），
当期新增帖子越少越空旷、越宜 meme 传播（流量）。

环境逐周计算两个无量纲指数（均以基线周 W12 = 1）：

- **存量丰沛度 B**：Stock = 过去 6 周 arena 供给总数（注入 + agent 帖，内生滚动窗口），
  B = f(Stock)/f(Stock_base)，f(x) = x/(x+K_a)。帖子总量越丰沛，梗的宿主池与素材土壤越厚。
- **流量空旷度 S**：Flow = 本段（现实口径）周新增帖量，S = h(Flow)/h(Flow_base)，
  h(x) = K_f/(K_f+x)。当期新增越少，注意力竞争越弱、未饱和传播空间越大。

涌现增益 G = clamp(B^β·S^σ, 0.2, 3.0)（β=σ=1 起步），仅进入玩梗型 Agent 的表达效用
（θ = 1；其余类型 θ = 0，环境不影响其决策）：

事件洪峰期（新增帖量极大）：

**S 低 → G 低 → 玩梗表达被淹没，发言概率降低**

热度退潮期（新增帖量回落、存量仍丰沛）：

**S 高、B 高 → G 高 → 玩梗表达被看见，发言概率提高，并可指数级扩张**

玩梗供给回升本身会加厚存量、抬高当期流量 → G 回落，形成自限反馈（logistic 式饱和）。

理论锚定：注意力经济（Simon 1971——信息的丰裕造成注意力的稀缺）、
生态位/logistic 增长（meme 需要未饱和空间才能指数扩张）、
模因论（Dawkins 1976——模因在丰宿主池中复制变异）。

该机制不改变 Agent 类型，而主要改变玩梗型 Agent 的表达时机。

这一机制将作为实验中的重要反事实研究对象。

核心反事实问题为：

**如果事件后的舆论场始终维持洪峰高热（空旷度不回升，sustained_hot 臂在事件周后冻结 S），后期的玩梗化公共表征是否仍然会出现？**

由此可以进一步区分：

- 玩梗内容的增长是否主要来自推荐算法的选择性放大；
- 是否同时依赖热度退潮释放出的未饱和传播空间。

---

### 2. 发言内容

Agent 类型在整个实验过程中保持固定。

每一种 Agent 只产生与自身类型相对应的评论或帖子：

- 玩梗型 Agent → 玩梗内容
- 悼念型 Agent → 悼念内容
- 教育观点讨论型 Agent → 教育观点讨论内容
- 借势营销型 Agent → 借势营销内容
- 其他类型 Agent → 其他讨论内容

LLM 负责生成具体的自然语言内容，但不得改变 Agent 所属的内容类型。

因此，本实验不模拟：

**Agent A 从“教育观点型”转变为“玩梗型”。**

而是模拟：

**在不同的信息环境中，不同类型的 Agent 谁更容易被激活、谁更容易沉默，以及谁产生的内容更容易获得进一步传播。**

最终研究的是不同既有表达群体在公共空间中的**相对可见性变化**。

---

## 三、Env 规则

Env 负责维护公共信息环境，并决定 Agent 在每一个 tick 中能够接触到哪些内容。

### 1. 传播 / 推荐算法

设置三种主要信息传播机制：

#### ① 随机推荐

Agent 从当前公共内容池中随机获得帖子。

该条件弱化用户兴趣和历史互动对信息曝光的影响，可作为较弱算法策展环境。

#### ② 时序推荐

帖子主要按照发布时间进行排序，较新的帖子具有更高可达性。

该条件用于模拟去个性化、弱策展化的信息环境。

#### ③ 兴趣推荐

系统根据：

- Agent 类型词表；
- Agent 历史曝光；
- Agent 历史互动；
- 帖子内容特征；

计算 Agent 与帖子的匹配程度，并优先推荐匹配程度较高的内容。

兴趣推荐环境允许出现：

**既有兴趣 → 相似内容曝光增加 → 相似群体互动增加 → 对应内容获得进一步传播**

的反馈过程。

三种算法环境之间保持其他实验条件尽可能一致，使“传播算法”成为主要实验变量之一。

---

### 2. 帖子投放

Env 中的帖子由两部分组成：

① 根据真实数据构建的外部内容；\
② 实验过程中由 Agent 新产生的内容。

真实数据主要用于为仿真提供与现实相符的初始内容环境以及每周外部内容输入。

Agent 产生的新内容则重新进入 Env，并可能在下一轮被其他 Agent 接触。

因此形成：

**Env 内容 → Agent 曝光 → Agent 发言 / 沉默 → 新内容进入 Env → 下一轮重新传播**

的循环结构。

真实数据的具体抽取比例、不同阶段的投放方式以及真实帖子与 Agent 生成帖子的比例，后续单独确定，以避免真实结果直接决定仿真结果。

---

### 3. 议程设置

官方媒体发布的重要事件信息被视为外生议程冲击。

当出现与张雪峰事件直接相关的重要官方媒体信息时，该类帖子具有最高可达性。

原则上：

**所有 Agent 均可以接触到重大官方事件信息。**

官方事件不依赖 Agent 的既有兴趣，因此可以暂时突破兴趣推荐形成的信息边界。

官方议程同时影响：

① Agent 对事件重要性的感知；\
② 当前公共意见气候；\
③ 玩梗涌现环境（洪峰压低空旷度）；\
④ 后续不同类型 Agent 的发言倾向。

---

## 四、每个 Tick 的运行过程

实验时间单位设置为：

**1 tick = 1 周，共 11 ticks（W12 生前基线周 → W22，对齐真实打标数据窗口）。**

每一个 tick 按照以下顺序运行。

### Step 1：Env 内容更新

Env 根据当前时间节点投入对应的真实数据内容。

如果本周存在重要官方媒体事件，则优先加入官方媒体帖子，并给予最高可达性。

同时保留符合规则的上一轮 Agent 生成内容。

### Step 2：算法推荐

Env 根据当前实验条件对应的传播算法：

- 随机推荐；
- 时序推荐；
- 兴趣推荐；

为每个 Agent 选择一定数量的帖子。

### Step 3：Agent 信息感知

Agent 阅读推荐内容，并更新：

- 当前意见气候感知；
- 自身类型在公共空间中的相对位置；
- 历史重复曝光程度；
- 当前注意力水平；
- 当前玩梗涌现环境感知（存量丰沛度 / 流量空旷度 / 涌现增益）。

### Step 4：Agent 发言决策

Agent 综合以下机制决定是否发言：

**沉默的螺旋 + 注意力衰减 + 玩梗涌现环境增益（U = D·R·G^θ，仅玩梗型受 G 门控）。**

Agent 最终产生：

**发言 / 沉默**

两种行为结果。

### Step 5：Agent 内容生成

如果 Agent 决定发言，则由 LLM 根据：

- Agent 固定类型；
- 当前看到的帖子；
- 当前事件环境；
- Agent 历史信息；

生成与其类型一致的具体内容。

Agent 不得生成其他类型的内容。

### Step 6：内容重新进入 Env

Agent 新生成的内容进入公共内容池。

下一 tick 中，这些内容与其他 Env 内容共同参与推荐和传播。

由此形成：

**算法推荐 → 信息曝光 → 表达 / 沉默 → 内容供给变化 → 下一轮算法推荐**

的动态反馈循环。

---

## 五、核心实验条件

实验首先围绕两个主要机制建立实验条件。

### 1. 推荐算法条件

分别运行：

① 随机推荐组\
② 时序推荐组\
③ 兴趣推荐组

比较不同推荐机制下五种公共表征的变化轨迹。

核心问题为：

**兴趣推荐是否会选择性提高某些既有表达群体的可见性，并最终造成公共数字表征结构发生变化？**

---

### 2. 玩梗涌现环境反事实（2026-09-10 机制重设计）

在主要推荐算法实验基础上进一步设置：

① 正常组（normal）：流量空旷度 S 随现实口径周新增帖量调度逐周演化（洪峰压抑、退潮释放）；\
② 反事实组（sustained_hot）：事件周后冻结 S，维持洪峰高热不退（存量丰沛度 B 保持内生）。

比较两组后期玩梗内容的增长程度。

核心反事实问题为：

**如果事件后的舆论场始终维持洪峰高热、未饱和传播空间不释放，算法是否仍然能够推动玩梗表征成为公共舆论中的显著表征？**

该实验用于识别：

**算法策展效应 × 玩梗涌现环境**

之间可能存在的交互作用。

**实验结构（2026-09-08 裁定 3×2 全因子；2026-09-10 第二因子换名）**：共 6 个实验单元（cell）：

| | 涌现环境正常演化（normal） | 涌现环境维持高热（sustained_hot） |
|---|---|---|
| 随机推荐 | random_normal（基线） | random_sustained_hot |
| 时序推荐 | chrono_normal（基线） | chrono_sustained_hot |
| 兴趣推荐 | **interest_normal（主治疗 + 生成性验证）** | **interest_sustained_hot（核心反事实）** |

交互作用由此可直接估计。除算法与涌现环境开关外，各 cell 的 Agent 群体、种子、初始内容与外部投放完全一致。

**重复次数**：每个 cell 运行 **3 个 seeds**，共 18 runs（100 agents × 11 ticks × 18 ≈ 1.98 万 agent-ticks）。探索性优先：先看形态涌现与组内离散度，效标匹配通过后可追加 seeds。

---

## 六、核心观测指标

实验最终不以单个 Agent 是否“改变观点”为主要结果，而观察公共内容环境中不同数字表征的相对可见性。

主要指标包括：

### 1. 各类型内容占比

每一个 tick 统计：

- 玩梗内容占比
- 悼念内容占比
- 教育观点内容占比
- 借势营销内容占比
- 其他内容占比

形成五类公共表征随时间变化的曲线。

### 2. 各类型 Agent 发言率

统计：

**某类 Agent 中实际公开发言的 Agent 数 / 该类型 Agent 总数**

用于判断公共表征变化究竟来自：

- 某类 Agent 更频繁表达；
- 某类 Agent 更频繁沉默；
- 推荐系统给予某类内容更高曝光。

### 3. 各类型内容曝光占比

统计每一类型内容实际获得的推荐曝光比例。

通过比较：

**群体规模 → 发言比例 → 内容供给比例 → 曝光比例**

判断推荐算法是否对特定数字表征产生选择性放大。

---

## 七、实验总体机制

整个实验关注的不是个体认知如何改变，而是固定表达群体在算法环境中的差异化可见性。

整体机制可以概括为：

**既有表达群体**

↓

**算法决定不同 Agent 看见什么**

↓

**意见气候、注意力衰减与玩梗涌现环境共同影响谁选择公开表达**

↓

**不同类型 Agent 产生不同数量的公共内容**

↓

**推荐算法再次对这些内容进行选择与放大**

↓

**不同表征在公共舆论场中的相对可见性发生变化**

↓

**形成公众人物身后的公共数字表征转移**

因此，本研究主要考察的是：

**推荐算法是否通过对既有表达群体及其内容进行差异化曝光和放大，使原本并未发生群体类型转换的社会环境，在公共可见层面呈现出显著的数字表征转移。**

---

## 八、配置裁定记录（2026-09-08）

本节记录在实验配置前由研究者裁定的四项关键决策，作为后续配置与复现的依据：

1. **推荐机制：纯兴趣匹配，不引入帖子级热度/互动信号。** 兴趣推荐的匹配度仅由 Agent 类型词表 × 帖子内容特征及 Agent 历史曝光/互动决定。这是个性化策展的最小机制：若转移不能涌现，则反证热度信号是放大必要成分。平台内置推荐算法（random / chronological 可用；内置 mf 模型为协同过滤、含热度泄漏）不满足本定义，需以自定义环境模块实现。
2. **Agent 群体比例：发帖人口径**（玩梗 18 / 悼念 21 / 营销 26 / 教育 15 / 其他 20；2026-09-10 用户裁定，替代旧用户口径主导类型 41/21/13/13/12），理由见一.1。
3. **实验结构：3×2 全因子 6 cells**（非嵌套），交互作用直接可估，见五.2。
4. **规模预算：11 ticks（W12→W22）× 每 cell 3 seeds = 18 runs**。真实数据止于 W22，不延伸至 W23。

另：事件/无事件嵌套臂不纳入本设计；内部因果识别由玩梗涌现环境反事实承担（2026-09-10 机制重设计，替代悼念规范压力反事实）。该线索保留于 TOPIC.md 假设结构表备查。
